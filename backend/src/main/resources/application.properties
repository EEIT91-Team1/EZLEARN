spring.application.name=backend

spring.datasource.url=jdbc:mysql://localhost:3306/ezlearn?useSSL=false&serverTimezone=Asia/Taipei&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.show-sql=true

server.servlet.session.cookie.same-site=NONE
server.servlet.session.cookie.secure=true

# 郵件服務主機
spring.mail.host=smtp.gmail.com
spring.mail.port=587

# 郵件服務帳密
spring.mail.username=mem61212phis@gmail.com
spring.mail.password=mgra ubnn kgdc slet

# 傳輸協定
spring.mail.properties.mail.transport.protocol=smtp

# 是否向郵件服務驗證身份
spring.mail.properties.mail.smtp.auth=true

# 是否啟用 TLS（傳輸層安全），對通訊加密
spring.mail.properties.mail.smtp.starttls.enable=true